{
  "name": "fit-persona-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "test": "../node_modules/.bin/vitest run --config vitest.config.ts",
    "deploy": "npm run build"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
